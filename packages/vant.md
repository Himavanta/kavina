# vant

基于 Vant UI 的移动端部件包。

## 部件列表

| 部件          | 说明   |
| ------------- | ------ |
| `vant-button` | 按钮   |
| `vant-popup`  | 弹出层 |

## 使用

```ts
import { vant } from "kavina/packages/vant";

const widgets = Object.values(vant);
```

## 前置

需要安装 Vant：

```bash
npm install vant
```
